import { LoginForm } from '@/components/admin/login-form.client';
import { hasSupabaseEnv } from '@/lib/supabase/env';
export default function AdminLoginPage({searchParams}:{searchParams:{setup?:string;error?:string}}){const configured=hasSupabaseEnv();return <main className="min-h-[70vh] bg-gray-50 px-4 py-20"><div className="mx-auto max-w-md rounded-xl border border-gray-200 bg-white p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-brand-secondary">Administration</p><h1 className="mt-2 text-3xl font-bold text-gray-900">Admin sign in</h1>{!configured?<p className="mt-4 rounded-md bg-amber-50 p-3 text-sm leading-6 text-amber-900">Supabase is not configured yet. Add your project values to <code>.env.local</code>, run the SQL setup and create an admin user before signing in.</p>:null}{searchParams.error?<p className="mt-4 rounded-md bg-red-50 p-3 text-sm text-red-800">Your account is not authorised for the admin panel.</p>:null}{configured?<LoginForm/>:null}</div></main>}
