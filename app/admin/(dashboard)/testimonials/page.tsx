import { AdminHeading, buttonClass, inputClass, Panel } from '@/components/admin/admin-ui'; import { requireAdmin } from '@/lib/admin'; import { createTestimonial, deleteTestimonial, updateTestimonial } from '../actions';
export default async function Testimonials() { const { supabase } = await requireAdmin(); const { data } = await supabase.from('testimonials').select('*').order('created_at', { ascending: false }); const rows = data ?? []; return <div className="space-y-6"><AdminHeading title="Testimonials" text="Create, edit, feature and remove learner or client feedback." /><Panel title="Add testimonial"><form action={createTestimonial} className="grid gap-3"><div className="grid gap-3 md:grid-cols-2"><input name="name" required placeholder="Name" className={inputClass} /><input name="company" placeholder="Company or programme" className={inputClass} /></div><textarea name="review" required placeholder="Review" className={inputClass} /><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" name="featured" />Feature on homepage</label><button className={`${buttonClass} w-fit`}>Add testimonial</button></form></Panel><Panel title="Testimonials"><div className="grid gap-3">{rows.map((item) => <form action={updateTestimonial} key={item.id} className="grid gap-3 rounded-md border border-gray-200 p-3"><input type="hidden" name="id" value={item.id} /><div className="grid gap-3 md:grid-cols-2"><input name="name" defaultValue={item.name} className={inputClass} /><input name="company" defaultValue={item.company || ''} className={inputClass} /></div><textarea name="review" defaultValue={item.review} className={inputClass} /><div className="flex flex-wrap items-center gap-4"><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" name="featured" defaultChecked={item.featured} />Featured</label><button className="text-sm font-bold text-brand">Save</button><button formAction={deleteTestimonial} className="text-sm font-bold text-red-700">Delete</button></div></form>)}</div></Panel></div>; }
